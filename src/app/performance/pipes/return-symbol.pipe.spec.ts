import { ReturnSymbolPipe } from './return-symbol.pipe';

describe('ReturnSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new ReturnSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
